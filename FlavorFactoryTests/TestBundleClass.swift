/// Dummy class to reference the test bundle for loading resources (e.g., test images).
/// Required because Bundle(for:) expects a class, and SwiftTesting structs do not provide one.
class TestBundleClass {}
